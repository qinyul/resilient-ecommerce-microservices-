global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'otel-collector'  
    static_configs: 
      # Prometheus scrapes the single endpoint exposed by the OTel Collector
      - targets: ['otel-collector:8889']
